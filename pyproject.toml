[project]
name = "DB_Connector_Pro"
version = "0.1.0"
description = "SQL-Connection handling with Python made easly, Serializiation Connect-Data, firststep OOP-Pattern"
readme = "README.md"
authors = [
  {name = "Richard Bieligk", email = "richard.bieligk@gmail.com"}
]
maintainers = [
  {name = "Richard Bieligk", email = "richard.bieligk@gmail.com"}
]
classifiers = [
    # TODO
]
license = {text = "MIT"}
dependencies = [
  "typer"
]
requires-python = ">= 3.10"

[project.optional-dependencies]
test = [
    "coverage",  # testing
    "pytest",  # testing
    "ruff",  # linting
    "ty", # checking types
    "ipdb", # debugging
]

[project.urls]
bugs = "https://github.com/Fritte1982/DB_Connector_Pro/issues"
changelog = "https://github.com/Fritte1982/DB_Connector_Pro/blob/master/changelog.md"
homepage = "https://github.com/Fritte1982/DB_Connector_Pro"

[project.scripts]
DB_Connector_Pro = "DB_Connector_Pro.cli:app"

[tool.ty]
# All rules are enabled as "error" by default; no need to specify unless overriding.
# Example override: relax a rule for the entire project (uncomment if needed).
# rules.TY015 = "warn"  # For invalid-argument-type, warn instead of error.

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # Pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "UP",  # pyupgrade
]

[tool.uv]
package = true
